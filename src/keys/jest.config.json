{
    "testEnvironment": "jest-environment-jsdom",
    "testMatch": ["**/**/*.test.j(s|sx)"],
    "verbose": true,
    "transform": {
        "^.+\\.jsx?$": "babel-jest"
    },
    "transformIgnorePatterns": ["node_modules/?!(@-0)"],
    "watchPathIgnorePatterns": ["^.+\\.repl.js$"]
}
